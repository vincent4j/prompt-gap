# M4 联调与交付进度

状态：待最后交互验收。四次真实调用的两份完整报告已获用户明确验收；个人 Skill 已安装并校验，安装版菜单程序已在隔离项目验证。客户端输入框候选发现和实际调用仍待用户验证。

## 本轮已验证

- 44 项测试通过：原 43 项，加真实测试脚本的双路径进度覆盖回归测试。
- 官方 Skill 格式检查通过。
- 白名单打包 13 个文件，附逐文件哈希清单。包含 Skill、元数据、运行代码、六份按需说明、通用 API 合成演示与示意图；不包含用户项目日志、生成报告快照、私人图片、凭据和本机路径。
- 在临时隔离目录解包，用包内脚本运行菜单、启用、关闭、恢复及离线持久化演示；引用路径指向解包位置，不依赖开发仓库。
- 安装包：[本地候选包](../../dist/prompt-gap-m4-candidate.zip)。SHA-256：`61ffaf0a124505925edcc09f58dc4ec419bf9c672e25aabf4ddd525da1b24806`。

## 安装前安全检查

- 来源：本次开发的本地 PromptGap 仓库；当前 GitHub 提交仍是初始化版本，不能用远端旧版本冒充本地完成版本。没有查询公开下载量/星标，这些不适用于本地自有候选包。
- 已审阅包内 13 个文件。未发现网络上传、读取凭据、混淆、外部输入代码执行或后台生图行为。动态导入仅指向本包固定脚本。
- 文件权限范围：用户选定项目的 AGENTS.md、.gitignore、日志/报告目录，明确提供的输入输出文件，以及本地临时锁和菜单文件。开启项目必须由用户选择。
- 网络：记录器不联网；能力研究需要用户单独选择，由宿主工具执行。
- 运行依赖：Python 3.10+；Pillow 可选。检查器使用的 PyYAML 不是运行依赖，不打包、不自动安装。
- 风险等级：中等（本地文件读写）。结论：可进行受控安装；不是安全防篡改系统，脱敏不保证发现所有供应商自定义秘密。

## 未完成项，不能算通过

| 项目 | 当前状态 | 下一步 |
|---|---|---|
| 用户级安装、下一轮发现 | 已安装，13 个文件哈希一致，格式检查通过 | 下轮加载；客户端发现单独验收 |
| `/P` 客户端实际发现 | 未验证 | 安装后新一轮确认；不能用元数据检查冒充 UI 验证 |
| 内置文生图 | 通过本次留档测试 | 原图、实际提示词、响应和完整报告已保存；返回未披露具体模型版本 |
| 内置图生图 | 通过本次留档测试 | 底图与上轮关联；改蓝成功，严格保留细节部分符合 |
| API 文生图 | 通过本次留档测试 | Sunburst 同步请求、原始响应及图片已保存 |
| API 图生图 | 通过本次留档测试 | 实际上传图片与源文件字节一致；改蓝成功，严格保留细节部分符合 |
| 真实开销与可阅读报告 | 报告用户验收通过；开销部分通过 | 留档函数计时已测；总工作流与 Token 开销未可靠测量 |

真实测试使用独立测试项目和简单原创素材，不重开 Poster Fit 的冻结商品。首次预算最多四次生成提交，不自动计费重试；失败、未知或下载问题先保留事实再决定下一步。实际执行前必须取得明确生成授权及 API 费用上限，并核对可用模型与当时价格。

## 2026-09-16 真实联调证据

本地测试入口：`output/m4-live-20260916/验收报告.md`（被 Git 忽略，包含私人本机路径，不进入可分享安装包）。状态清单、完整原始响应及校验结果同目录；逐次报告在 `reports/api/report.md` 和 `reports/codex_builtin/report.md`。两项任务各两次调用，完整性检查均无问题；API 原始响应解码与交付 PNG 字节一致。

| 路径 | 文生图调用耗时 | 编辑调用耗时 |
|---|---:|---:|
| Codex 内置 | 17.153 秒 | 25.544 秒 |
| Sunburst API | 26.812 秒 | 48.693 秒 |

调用耗时不等于端到端耗时，API 编辑不含此前上传；单次样本不用于模型速度排名。每次开始与结束留档函数合计约 9.95～21.15 毫秒，仅限本地函数，不含工具往返、Agent 整理、上传和报告阅读。两份报告脚本各约 14 毫秒，**不含人工式看图分析和编写诊断耗时**。

实际费用、Token 用量未知。[官方公开标价](https://docs.clawpowerai.com/api/images/gpt-image-2-5-sunburst)对应本轮两次 1K API 调用合计约 0.20 元，但未查实际账单。没有额外评审模型调用。首轮内置响应检查误把大段图像编码输出到工具上下文，随后改成仅输出路径和计时；本轮不能作为 Token 节省达标证据，不为测量重新付费生成。

并行测试发生一次测试脚本进度汇总覆盖；原始调用记录和图片未丢失。从现有记录恢复汇总，增加锁内合并并用无网络测试验证，无重复调用。此修改仅作用于 M4 测试脚本，不改生图 Skill。

个人 Skill 已安装，未给其他项目启用留档，未修改 Poster Fit、未提交/推送、未公开仓库或指定许可证。异步生产调用、跨供应商全面覆盖、安装后的自动遵循和 `/P` 客户端发现均不能据此宣称通过。本文件是续接入口，不是 M4 完工声明。

## 本轮安装交付

用户明确通过两份完整报告后要求继续安装及菜单验收。本次对 13 个运行文件重新安全审阅，未发现网络传输、凭据读取或后台生成；仅选定项目文件、指定素材与临时锁/菜单读写，中等文件操作风险，可受控安装。公开星标/下载量不适用于这个本地自有包。

安装器现成脚本只支持 GitHub 来源，本次使用本地审核包解压到个人 Skill 目录，不覆盖已有安装。新包 `dist/prompt-gap-m4-install.zip` 哈希：`cd850be0e210adedb1d96bf16af50adcb10f2e11f1c2967173841ec7bd2e87f8`。原候选包保留，不再作为最新交付版本。

44 项测试通过；安装版额外验证：未启用/已启用/关闭恢复、八项历史记录分页、新任务插入后旧编号仍指向原任务、关闭后历史保留、项目规则指向安装目录。官方格式检查通过（使用本机离线缓存的检查依赖，不新增 Skill 运行依赖）。

下一步仅需用户输入 `/p` 选中 PromptGap 并发送，核对真实菜单；不再生图。[安装与使用](../安装与使用.md) 已补齐。
