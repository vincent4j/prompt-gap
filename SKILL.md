---
name: prompt-gap
description: 用户主动选择 PromptGap 时，开启或关闭当前项目的图像生成事实留档，按数字选择历史任务，查看用户需求、当时理解、实际提示词、输入素材与返回结果。适用于了解成功过程或排查多次生成偏差；同时支持 Codex 内置生图及任意 API。
---

# PromptGap

提供轻量留档、数字菜单与按需审核报告。不要把合成样例当成用户实际生成报告。

## 用户入口

1. 明确当前项目根目录，运行 `python3 <skill-dir>/scripts/prompt_gap.py --project <project> menu`。没有项目目录时只问一次工作目录；不要默认为 Skill 仓库。
2. 展示返回的项目状态和数字选项，仅展示 label，隐藏内部 ID、路径与命令。保留本次 menu_id。
3. 用户回复数字后，用 `select` 传入 menu_id、choice。按返回 action 执行，不重新编号，不用全局“最近任务”替代已选任务。无效/过期菜单重新展示。
4. 操作映射见 [菜单交互](references/menus.md)。启用会修改本项目 AGENTS.md、.gitignore 并创建本地配置；在启用选项说明中告知这一点，用户选择该项即授权该范围。关闭保留历史。

## 留档

记录之前完整读取 [执行留档](references/capture.md)。API 另读 [通用 API 记录](references/api-recording.md)。结构语义见 [记录协议](references/recording.md)。

- 不接管生图工具、不维护供应商名单、不新增评审模型调用。CLI 只做本地文件操作。
- 项目启用后由 Agent 遵守 AGENTS.md，在实际调用前 begin、返回后 finish；不是后台拦截器，不能保证其他 Agent/客户端都执行。
- 用户原话和当时理解分别记录。继承要求必须保留；不事后编造当时理解。补录用 backfilled，演示用 synthetic。
- 每次真实重新提交建立新 attempt；轮询、下载重试只是事件。不要因为记录失败而重复付费调用。
- 保存实际可见载荷和输入顺序；看不到的模型版本、内部提示词、费用、素材一律显式缺失。脱敏副本不得声称逐字完整。
- 只追加反馈/观察，不倒改原始事实。日常返回简短留档状态，不把整份 JSON 倒回对话。

## 查看报告或记录

用户选择审核时完整读取 [按需审核](references/review.md)，由当前 Agent 看图和比较证据，脚本校验关联并生成有版本的 report.md、details.md。直接交付的 report.md 必须包含完整逐次过程，不能只给摘要让用户跳详情补上下文。没有额外评审模型调用。

成功和失败一律按同一完整模板：当次完成情况/问题 → 用户原话与继承要求 → 当时理解 → 完整实际提示词和图片 → 实际返回 → 逐项数据流转与审核依据 → 风险和下一步。成功不缩成“符合”，缺失节点明确缺失，不捏造过程。证据不足不填确定归因；脚本不证明 Agent 的语义判断正确。

面向人阅读的状态、来源、类型、职责、字段说明和缺失原因使用简体中文，不直接展示内部英文枚举或字段路径。实际发送的提示词、请求和响应保留原文；英文原始证据另附中文解释，不篡改底层记录。

用户明确选择调研后才读取 [模型能力研究](references/research.md) 并执行，不自动联网、重新生成或换模型。用户纠正审核时追加反馈并生成新报告，旧记录和报告保留。

包内样例使用合成素材，无真实模型调用。开发仓库已验证内置/API 各一次生成及编辑的记录链路，但不代表所有入口都自动接入；安装后的客户端发现和具体项目覆盖仍需分别确认。
